# PochiDoku engagement MVP status

- `E1` — **in_progress** — Add deterministic puzzle generation and backward-compatible daily metadata. Verify build-level Swift syntax and stable seed flow.
- `E2` — **pending** — Add local daily completion/streak persistence and personal percentile calculations. Verify date/streak edge cases by inspection and focused assertions where practical.
- `E3` — **pending** — Integrate daily lifecycle, fixed mistake limit, and live score in `GameViewModel`. Verify regular, resume, daily, loss, and completion paths.
- `E4` — **pending** — Update Home, Game, Completion, and Stats UI with a clean engagement layer. Verify compact layout and accessibility labels.
- `E5` — **pending** — Update documentation and screenshot seed. Verify privacy copy remains accurate.
- `E6` — **pending** — Run final diff review, GitHub Actions build, simulator screenshot, and publish the verified screenshot/PR update.
